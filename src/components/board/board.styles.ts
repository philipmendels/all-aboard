import { style } from "typestyle";

const rootClass = style({
  backgroundColor: 'white',
  position: 'relative',
  width: '100vw',
  height: '100vh',
  outline: 'none'
});

export const boardStyles = {
  rootClass
}
